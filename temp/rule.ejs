/**
 * <%= name %>
 */

module.exports = {
  meta: {
    docs: {
      description: '<%= name %>',
      category: 'Fill something',
      recommended: true,
    },
    // fix code: use "code" or "whitespace"
    fixable: null,
    schema: [],
    messages: {
      avoidMethod: 'tip something',
    },
  },

  create(context) {
    
    return {
      // visitor functions for different types of nodes
    };
  },
};
